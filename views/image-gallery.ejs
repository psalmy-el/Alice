<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Image Gallery</title>
    <link rel="stylesheet" href="/css/image-gallery.css">
</head>
<body>

    <header>
        <div class="header-inner">
            <a href="/" class="logo"><img src="https://res.cloudinary.com/dawxl838a/image/upload/v1746869871/WhatsApp_Image_2025-03-30_at_19.48.32_d7dd8868_pudffo.jpg" alt="Engdahls & Co"></a>
            <button class="nav-toggle">☰</button>
            <nav class="main-nav">
                <button class="close-nav">✕</button>
                <ul>
                    <li><a href="/">Home</a></li>
                    <li><a href="/contact">Contact</a></li>
                </ul>
            </nav>
        </div>
    </header>
    <div class="container" style="margin-top: 120px;">
        <div class="gallery-container">
            
            <div class="main-image-container">
                <div class="slide-container" id="slide-container">
                    <!-- Initial slide will be inserted here by JavaScript -->
                </div>
                <% if (media.files.length > 1) { %>
                    <div class="arrow left" id="main-prev">&#10094;</div>
                    <div class="arrow right" id="main-next">&#10095;</div>
                <% } %>
            </div>
            
            <% if (media.files.length > 1) { %>
                <div class="thumbnail-wrapper">
                    <div class="thumbnail-container">
                        <div class="thumbnail-scroll" id="thumbnail-scroll">
                            <% media.files.forEach((file, index) => { %>
                                <div class="thumbnail <%= index === 0 ? 'active' : '' %>" data-index="<%= index %>">
                                    <img src="<%= file.file_path %>" alt="<%= media.title %> - Image <%= index + 1 %>">
                                </div>
                            <% }); %>
                        </div>
                    </div>
                </div>
            <% } else if (media.files.length === 1) { %>
                <!-- Add a hidden thumbnail for single image cases -->
                <div style="display: none;">
                    <div class="thumbnail active" data-index="0">
                        <img src="<%= media.files[0].file_path %>" alt="<%= media.title %>">
                    </div>
                </div>
            <% } %>
            
            <h1><%= media.title %></h1>
            
            <div class="media-info">
                <% if (media.description) { %>
                    <div class="media-description">
                        <p><%= media.description %></p>
                    </div>
                <% } %>
                
                <div class="media-metadata">
                    <span class="category-label">Category: <%= media.category_name %></span>
                </div>
            </div>
            
            <div class="back-link">
                <a href="/#video-grid-section" class="btn-back">← Back to Gallery</a>
            </div>
        </div>
    </div>
    
    <script src="/js/image-gallery.js"></script>
</body>
</html>